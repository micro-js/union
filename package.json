{
  "name": "@f/union",
  "description": "Compute the union of two arrays",
  "repository": "git://github.com/micro-js/union.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/unique": "^1.0.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
